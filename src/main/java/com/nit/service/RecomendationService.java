package com.nit.service;

public class RecomendationService {
//
//    private final UserRepository userrepository;
//   // private final ActivityRepository activiyRepository;
//
//    public Recommendation recommendationService(RecommendationRequest request) {
//
//        User user = userrepository.findById(request.getUserId())
//                .orElseThrow(() ->
//                        new RuntimeException("User not found: " + request.getUserId()));
//
//		/*
//		 * Activity activity = activiyRepository.findById(request.getActivityId())
//		 * .orElseThrow(() -> new RuntimeException("Activity not found: " +
//		 * request.getActivityId()));
//		 */
//        Recommendation recommendation = new Recommendation();
//
//      
//
//        return recommendation;
//    }
}